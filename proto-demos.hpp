#ifndef __DEMO_PROTOS
#define __DEMO_PROTOS

#include <vector>

// def ===parallelSearch.cpp===
long sequentialSearch(std::vector<int>, int);
long parallelSequentialSearch(std::vector<int>, int);

// def ===parallelFor.cpp===
long parallelFor();
long nonParallelFor();

#endif /* __DEMO_PROTOS */
